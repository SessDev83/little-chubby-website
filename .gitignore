# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.*
!.env.example

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# local agent/tool settings
.claude/settings.local.json

.vercel

# social engagement state files
scripts/social/.post-state.json
scripts/social/.post-history.json
scripts/social/.engage-state.json
scripts/social/.validation-failures.jsonl

# source image assets (too large for git)
Free Images/
Raw awesome girls revie muestra/

# operator-only temp files (auto-generated helpers — not part of repo)
_APPLY_*.sql

# local-only PowerShell helpers (contain env-specific refs / credential interop)
scripts/_*.ps1
scripts/temp-inputs/
scripts/temp-manuscripts/
_PIN_POSTS_*.txt
PIN_TO_COPY/
.commit-msg.tmp
astro-build*.log
astro-check*.log

# operator-only local artifacts
__schema_dump.sql
docs/*.pdf
/0
/f.endsWith('.md'))
/tags.includes(t)))
/tags.includes(t)))counts\[k\]++
/.claude/scheduled_tasks.lock


# Internal planning / operations docs (not for public repo)
docs-internal/

# local reference/training PDFs; public PDFs belong under public/downloads when intentional
/*.pdf

i18n.log
check.log
